name: Build ZMK firmware
on:
  push:
    paths:
      - 'config/**'
      - 'boards/**'
      - 'build.yaml'
      - 'zephyr/**'
  pull_request:
    paths:
      - 'config/**'
      - 'boards/**'
      - 'build.yaml'
      - 'zephyr/**'
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
